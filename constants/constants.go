package constants

// Name is the name for tiresias.
const Name = "tiresias"

// Usage is the usage for tiresias.
const Usage = "A tool to help you access your servers"

// CommentPrefix means tiresias's line started.
const CommentPrefix = "# -- "

// CommentSuffix means tiresias's line ended.
const CommentSuffix = " --"
